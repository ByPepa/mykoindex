# Jádro pipeline (nutné)
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
requests>=2.28

# Geodata / export
rasterio>=1.3      # GeoTIFF export, čtení DEM/masky
pyproj>=3.4        # reprojekce WGS84 <-> pracovní CRS (i ČHMÚ Mercator)
h5py>=3.8          # čtení ČHMÚ MERGE (ODIM_H5)
matplotlib>=3.6    # PNG overlay + náhledy

# Zdroje dat (volitelné pro --demo; nutné pro ostrý běh)
pandas>=2.0
xarray>=2023.1     # čtení ERA5-Land NetCDF
netCDF4>=1.6       # backend pro xarray
cdsapi>=0.6        # ERA5-Land přes Copernicus CDS
# GBIF: čteme přímo přes REST (api.gbif.org), pygbif není potřeba

# Testy
pytest>=7.0

# Volitelné (pokročilé, neimportuje se natvrdo):
# wradlib          # pokročilé slévání radar+srážkoměr
# scikit-learn     # logistická regrese / RF pro kalibraci
# elapid           # MaxEnt presence-background
